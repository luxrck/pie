# MEMORY.md — 持久记忆

本文件是项目的持久记忆：记录用户偏好、当前系统概览与踩坑记录。agent 在每次会话开始时读取，并可在运行中用 edit/write 更新。

保持简洁：只记跨会话仍然有效的事实，不要记临时状态。
历史关键决策与变更记录已移至 `docs/CHANGELOG.md`（本文件只保留当前状态）。

## 用户偏好

- 信奉 YOLO：不要权限确认，工具直接执行。
- 偏好极简、可扩展的实现。
- TUI 背景透明：露出终端窗口背景色（Ubuntu 终端默认深红色），不画自己的背景色。
- **本机有两份 pie 源码树，别搞混**（2026-09-16 查清，2026-09-21 复核）：① 本项目主树 `/mnt/d/pie-master`（`.venv` editable → `/mnt/d/pie-master/src`）；② 旧 checkout `/home/cc/src/pie`（termbg.py 还没并进 theme.py，已落后，别改）。**全局 `pie` 命令（`~/.local/bin/pie` → `~/.local/share/uv/tools/pie/bin/pie`）当前指向 ①**（2026-09-21 实测 `pie.__file__` = `/mnt/d/pie-master/src/pie/__init__.py`，已在 /mnt/d/pie-master 里跑过 `uv tool install . --editable`）——用户实际用的就是 ①，只需维护这一树。

## 当前系统概览

- **架构**：分层包（tools / llm / loop / config / chat / context / cli / aio），src 布局，可全局安装（`uv tool install . --editable`）。内置工具仅 read / edit / write / shell，`@tool()` 按签名生成 schema，LLM 协议可注入。
  `aio.py` = 事件循环收尾（`run()` 代替 `asyncio.run`、`close_asyncgens()`、`event_loop()`；见「踩坑记录 → 事件循环与异步生成器」）。
  **Session 的三个路径/集合字段**（名字要一眼分清）：`path` = 会话 JSONL 自身的文件路径；`windows` = `/clear` 归档的历史窗口块（落在 `~/.pie/windows/`）；`files` = 图片 id 表（hash_id → file_id/本地副本/过期时间，见 `files.py`，存进 `__meta__`）。旧名 `file` / `fs` 已于 2026-09 改名（`__meta__` 里的 `fs` 键仍兼容读）。
  **公共 API**：唯一入口契约是 `pie/__init__.py` 的 `__all__`（31 个）；各模块顶部也自带 `__all__`，内部模块（`aio` / `cli` / `clipboard` / `files` / `input` / `textkit` / `theme` / `tui` / `__main__`）为 `[]`（`__all__` 只约束 `import *`，不挡显式导入）。跑一轮两个入口：`pie.run(task, tools=/llm=/config=)`（同步一次性，内包 `aio.run`）与 `pie.aturn(messages, cfg, registry, backend, ...)`（异步主路径）；旧名 `run_agent` / `acomplete_turn` 已废（`Session.aturn` 是同名的会话层方法，内部按模块限定调 `loop.aturn`）。
- **CLI 与交互**：`/` 开头**但不是已知命令**的输入按普通消息发出（粘贴进来的绝对路径 `/home/.../x.png` 常被误伤，判据 = 首词在 `PALETTE_COMMANDS` 里，见 `PieApp.is_known_command`）。`pie`（新对话）/ `pie resume`（恢复最近）/ `pie` 带参数（一次性子 agent，不写 sessions）/ `pie context info|verify|gc`；`-c/--config` 指定配置。真实终端走 Textual TUI，非 TTY 回退 `cli.read_input`（prompt_toolkit，缺则内置 `input()`）；输入以 `!` 开头走 shell 模式（不进上下文）。运行时命令：`/stat`（用量）、`/thinking`（切换思考深度）、`/model <id>`（切换模型，启动自动拉取可用列表）、`/compact [tools|turns]` 手动压缩（无参 = 工具级+轮次级）、`/stop` 或 `Esc`（取消当前任务：补全面板开着时 Esc 只收起面板）、`/clear`（切换窗口）。
- **工具**：所有内置工具输出统一为 `Headers\n\nBody`（headers 一行一个 `[...]`，body 空则省略空行；状态/元数据放 header 区，如 shell 首行 `[exit=..]`、shell 截断时 `[工具输出全文已保存: path]`、read 截断时 `[已截断：可用 offset=N 继续读]`、行号 `[行 a-b，共 N 行]`）。read 支持 offset/limit 分页 + 图片多模态 + 私有容量上限（_max_lines/_max_bytes/_max_image_bytes，截断只报续读提示、**不落盘**）；edit 为 edits 数组（oldText 唯一、防重叠、按原文非增量）；shell 支持私有容量上限（超限只保留尾部，全文落盘指针）。
- **上下文压缩**：三级——工具级（按行保留 head+tail 落盘指针）/ 轮次级 / 会话级，摘要全部规则式（零模型调用）。工具级保护窗口由 `keep_last_steps` 决定（最近 N 个 step 批次，跨轮次滚动）；轮次级只保护当前轮、已完成轮次可压。内容 hash 落盘 + 指针链 + 压缩级别只升不降 + 软阈值 / 目标水位迟滞（比例在 `[compaction]` 的 `soft_ratio` / `target_ratio`，**相对可用输入预算 = `context_window` - `reserved_tokens`**，默认 80% / 55%）；manifest 索引 + verify/gc。`compaction` 为显式配置：默认 None 不做任何压缩，写了则三级全开（子表调 head/tail，`tool/turn/session = false` 关闭对应级）。
- **模型层**：OpenAI 兼容接口，`reasoning_effort` 透传；`reasoning_content` 捕获/持久化/原样回传（thinking 模式 400 已修）。采集 provider usage（prompt/completion）+ UsageTracker 跨 resume 累计。**重试自己实现**（`llm.py`：`_retryable` / `_retry_delay` / `_retry`，SDK 自带那套用 `max_retries=0` 关掉）——次数 = `1 + Config.max_retries`，等待 = `max(1.0, random(0, Config.max_retry_delay_seconds))`（默认值在 config.py：`Config.max_retries=2` / `Config.max_retry_delay_seconds=DEFAULT_MAX_RETRY_DELAY_SECONDS`；`OpenAILLM` 的这两个参数**不传就是 0**，组件层面默认不重试）；**上下文账**：`context_window`（模型窗口，输入 + 输出一起算）+ `reserved_tokens`（每次请求为输出预留，就是发给 API 的 `max_tokens`，`None`/`"auto"` = 不发、用服务端默认）→ `Config.context_budget() = 二者相减`；`/stat` 与 system prompt 都按这份预算展示。
- **图片（`read` → 多模态注入）**：read 返回标记文本，harness 把图片作为**多模态 user 消息**注入（图片只能在 user 消息里）。默认走 **Files API**：先落本地内容寻址副本 `~/.pie/files/<hash_id>`（`hash_id = img-<sha256[:16]>`）→ **从副本上传**拿 `file_id` → 历史里只留 `{"type":"file","file_id":…}`。记录按会话存 `__meta__.files`（无全局缓存）；未命中/过期/换 key/模型不支持/上传失败 → 静默回退内联 base64。维护：`pie files list [--all]` / `pie files gc [--delete] [--all]`（**副本目录也是剪贴板粘贴的落点** → 本地 gc 另有 24h mtime 保护窗口，见 `clipboard.py`）；两个 `--all` 都直接调 Files API 看/清**云端**（`files.list_remote_files` / `purge_remote_files`，云端是**账号全局**的：list 用本地会话记录标注 `会话=未记录`，gc 删本账号**全部**上传件），本地副本/会话记录不动、靠 loop 降级自愈。⚠️ `Config().api_key` 默认值是本部署真实 key —— 写测试时「配置没写 api_key」不等于空 key，漏 stub 会真的删线上文件。
- **剪贴板图片 → 路径（`clipboard.py`）**：输入框 `Ctrl+V`（或 `/paste`）把剪贴板里的图片编码成 PNG 后交给 **`files.store_blob()`**，返回 `~/.pie/files/img-<sha256[:16]>.png`（**就是 `read` 用的那份副本** → 回车后 read 命中同一文件、**零复制**）并把**路径**插进输入框（回车即普通 `read`）。后端是 Pillow `ImageGrab.grabclipboard()`（同步阻塞 → TUI 侧 `asyncio.to_thread`）：**Windows/macOS 开箱可用**，**Linux 需 `wl-clipboard` 或 `xclip`，两者都缺时 Pillow 抛 `NotImplementedError`** → 静默当作「没有图片」。没图时 Ctrl+V 回退原本的文本粘贴（覆盖的是 `TextArea.action_paste`，绑定不变）；Windows 剪贴板里是图片**文件**（CF_HDROP）→ 返回原路径、不复制。共目录的代价：`files.gc` 的「未被引用 = 垃圾」对刚粘贴还没 read 的图不成立 → **mtime 保护窗口 `GC_PROTECT_HOURS`（24h）**。按键：Ctrl+V（Textual TextArea 自带绑定）+ **Ctrl+G**（App 级绑定，终端截走 Ctrl+V 时的兜底）+ `/paste` 命令，三者同一实现；WSL 另有一条 **PowerShell 后备**（`Clipboard::GetImage()`，`-sta` 必加，路径走 `wslpath -w`），**只在 Pillow 抛 NotImplementedError（没装 wl-paste/xclip）时**才跑——否则「剪贴板里没有图片」也要白等一次 0.4s PowerShell 冷启动。
- **一套主题适配深色/浅色终端（主题族 + 终端背景探测）**：`theme.py` 的 `detect_dark_background()`（**原 `termbg.py`，2026-09-20 整并进 theme.py**；进程内缓存）按 OSC 11 查询 → `COLORFGBG` → None 的顺序判断终端背景明暗（OSC 11 直接读写 `/dev/tty`，临时关规范模式/回显、0.2s 超时、读完恢复；纯函数 `parse_osc11` 认 `rgb:`/`rgba:` 的 2/4 位分量，按 sRGB 加权亮度 < 0.5 判深色；`parse_colorfgbg` 认背景索引 < 8 为深色）。`theme.py` 引入**主题族**：`THEME_FAMILIES = {"catppuccin": (CATPPUCCIN_MOCHA, CATPPUCCIN_LATTE)}`，`get_theme(name, dark=None)` 对族名按明暗选变体（探测不到按深色），具体变体名（`catppuccin-mocha`）固定不受影响；`Config.theme` 默认 `"catppuccin"`，`PieApp.__init__` 探测一次并据此选 palette + Textual 主题（`ansi-dark`/`ansi-light`，两者 background 都是 ansi_default → 仍透明）。用户配置已由 `catppuccin-mocha` 改为 `catppuccin`（否则不会自适应）。
- **TUI 主题**：`theme.py` 集中所有展示数据（配色 + 图标，不依赖 Textual/Rich），四类取用：`Theme.role_border(role)`（未知 role 回退 system；`cancelled` 与 system 同色）、`Theme.role_icon(role)`、`Theme.tool_icon(name)`（**调用**框图标：`tool_icons` 按工具名覆盖，未配置回退 `icon_tool_call`，值 `""` = 无图标）、`Theme.result_icon(role, name)`（**结果**框图标：按 role 取状态字形，可被 `tool_result_icons` 覆盖）、`Theme.lean_mark(status)`（简洁模式行首标记，status ∈ ok/fail/cancelled，未知回退 ok）。
  **「执行结果」字形只有一套**：`icon_ok(✅)` / `icon_error(❌)` / `icon_cancelled(⏹)`——盒子模式作 role=tool_result/error/cancelled 的标题图标（`↳` 那套「这是上一个框的产出」已废），简洁模式同一套放行首；取哪个由 `_box` 按执行结果判定（退出码/失败前缀/取消）。角色轴与结果轴分开：**调用**框图标回答「调的是哪个工具」（`tool_icons` + `tool_result_icons` 是对结果字形的 opt-in 覆盖），结果框图标回答「结果如何」。默认主题：`tool_icons={"read": "✧", "edit": "⟱", "write": "✦", "shell": "❯"}`，`tool_result_icons={}`。原则：**视图层不再出现图标字面量**。渲染分层：**`_box` 只做分派** → 工具活动且 lean 走 `_lean`（单行 `list[Padding]`）、否则 `_panel`（盒子 `list[Panel]`）；两者参数一致、body/role 语义一致，但**各自自包含、不调中间小函数**（`_raw_panel` / `_tool_result_box` 已删）。边框色一律 `border_role` > `role`，**只有手动 `!cmd` 指定 `border_role`**（成功框用默认灰 `MANUAL_SHELL_ROLE`，同时保住 role 带来的 ✓/✗ 状态图标）。
- **TUI 滚动条一份样式**（2026-09-21）：`build_css` 里的 `scrollbar` 片段（`scrollbar-size: 0 1` + 半透明灰轨道 `scrollbar_track` + `muted` 滑块）写在 `#log` / `#assistant-stream` / **`#input`** 三处——`TextArea` 是 `ScrollView`，它的 `ScrollBar` 子控件读的是**父控件**（即 `#input`）的 `scrollbar-*` 样式，所以写属性就够了，不用给滚动条控件另写规则。以后新增可滚动控件（如 CommandPalette 若要）同样带上这段，否则会退回 Textual 默认的 2 cell 黑底蓝条。回归 `test_input_scrollbar_matches_log`。
- **输入框按键**（`PieTextArea`）：`Enter` 提交；换行 = `Shift+Enter` / `Ctrl+J` / `Ctrl+Enter`（`ctrl+enter` 只在支持修饰键上报的终端才独立送达，多数终端把它编码成 LF = `ctrl+j`）；`Tab` 接受补全、`↑/↓` 切候选、`Esc` 先收面板再当 `/stop`、`Ctrl+V` 图片粘贴。回归 `test_input_newline_keys`。
- **TUI 显示**：断行是 CJK 友好的——tui.py 导入时调 `textkit.install_cjk_wrap()`（全角字逐字可断、英文词不断，纯 ASCII 交回 Rich 原实现）；`tui.py` 的 `SelectableRichLog` 框选复制按「源文本 + 显示行对齐」切来源文本（不是拼显示行），长行复制为一行。tui.py 只做应用编排（布局/命令/回合 worker/流式），渲染**只有一个出口 `_notify(body, role="system", **kw)`**（= 把 `_box` 的产物写进 #log，唯一调用 `log.write` 的地方），而 **`_box(palette, body, *, role="system", border_role="", lean=False)` 只做分派**：lean 且 role 是 `tool_call` / `tool_result` → `_lean`（单行，`list[Padding]`），否则 `_panel`（盒子，`list[Panel]`）。**`_panel` / `_lean` 参数一致、body/role 语义一致，且各自自包含**（参数→文本、参数→摘要、状态判定、`[exit=]` 解析、标题图标推导、截断、清洗都 inline 在函数里，不调任何中间小函数；代价是前两项在两者各一份）。`role` 只有三类取值，**工具名写在 body 里**：消息类 → 正文；`tool_call` → `{"name", "arguments"}`；`tool_result` → `{"name", "arguments", "content"}`（arguments = 配对那次调用的参数，供 `_lean` 在内部按 `LEAN_SUMMARY_KEYS` 推摘要）。盒子模式的**工具正文**超过 `BOX_BODY_LINES`（24）行时只显示前 N 行；消息正文不截。**异常显示**：`_error_body(exc)`（`_fail_turn` / `!shell` 兜底共用）= 首行 `类名: 消息` + `↳ 异常链`（`_exc_chain` 走 `__cause__`，SDK 包装层的真原因——DNS/连接/TLS——都在这里；不再按类型另加提示）。
- **TUI 简洁模式**（`[tui] lean = true`）：只给 user / assistant 套盒子，工具活动交给 `_lean` 压成单行 `Text`（`[状态标记|工具图标] 工具名 摘要`——结果行 ✅/❌/⏹ 在**行首**，失败/取消时下方 `↳ 正文` 续行），外面套 `Padding(text, (0, BOX_INSET))` 与盒内正文对齐。**失败正文块的截断与 `_panel` 同一条规则**：超过 `BOX_BODY_LINES` 行只显示前 N 行 + 同一句 `...[已省略 K 行，共 M 行]...`（`LEAN_DETAIL_HEAD/TAIL` 已删，别再另搞一套；改渲染后跑 `tests/test_tui.py`）。`BOX_INSET` 由盒子几何推导（`_BOX_BORDER + _BOX_PADDING[1]`，与 `_panel` 共用 `_BOX_PADDING`）——别另写 2；`#log` 的 CSS padding 与这件事无关（同一内容区，改它不破坏对齐）。标记放行首是关键：`Text(no_wrap=True, overflow="ellipsis")` 的右截断天然保住它，所以没有自定义 renderable（`_LeanLine` 已删；摘要要先压成单行：真换行在 `no_wrap` 下照样断行、tab 会撑破宽度——这段 inline 在 `_lean` 里）。状态标记字形在主题里（`icon_ok/icon_error/icon_cancelled` + `palette.lean_mark(status)`，**只认 ok/fail/cancelled 三个词，传 role 名会静默退回 ok**；`_lean` 里有一张 status→它的小映射表）；单行摘要从 `arguments` 推（`LEAN_SUMMARY_KEYS`，见上条）。改动 lean 渲染后跑 `tests/test_tui.py`（`test_lean_tool_lines_are_padded` 含「框选复制不带留白」断言）。**唯一例外：手动 `!cmd`**（`_run_shell` / `_show_shell_result`）不吃简洁模式——它是用户主动执行、输出本身就是要看的东西，任何模式都套盒子：命令行回显 = `tool_call` 形状（`arguments` 位置放 `$ cmd`）+ `border_role=MANUAL_SHELL_ROLE` + `lean=False`；结果框 = `role="shell"` + 自行补 `[exit=N]` 头 + `border_role` 只在成功时给默认灰（失败红/取消灰由状态推），`_panel` 的 `border_role` 参数实际上只给这条路用。
- **依赖（direct，5 个）**：`openai`（核心）/ `textual` + `rich`（TUI）/ `pillow`（剪贴板图片）/ `prompt_toolkit`（非 TTY 行编辑）。`numpy` 于 2026-09 移除：声明了但全仓库零引用、也不是任何传递依赖（`uv tree` 确认），删后 `uv lock` 29→27 包、`.venv` 123M→68M，全部 73 例测试通过。⚠️ **`pillow` / `prompt_toolkit` 是刻意的软依赖、别误判为可删**：代码里都是 try-import 降级（`clipboard._engine()` 缺 Pillow → 剪贴板图片功能静默不可用；`cli.py`（原 `input.py`，2026-09-20 并入）缺 prompt_toolkit → 回退内置 `input()`，WSL/mintty 中文退格会截断），静态扫描看起来“只在一处用到”就删会让功能悄悄失效。
- **配置**：只从 `~/.pie/config.toml` 读取（保留 `PIE_DIR` / `PIE_CONFIG_FILE` 路径重定向）；`Config.tools`（dict，按工具名设默认私有参数，如 read: {_max_lines,_max_bytes,_max_image_bytes}）由 dispatch/adispatch 注入下划线私有参数（`_inject_tool_defaults`，只注入下划线且不覆盖显式传参）；SYSTEM.md / AGENTS.md / MEMORY.md / `~/.pie/memory.md` 存在即注入 system prompt；`Config.parallel_tools`（默认 True）= 同一批 tool_calls 是否并发执行（`aturn(parallel_tools=...)` 不传时跟随它，工具共享可变状态时必须设 False）。

## Rust 重构（pie-rs）

- **位置与定位**：`pie-rs/`（Cargo 项目，与 Python 版并存；**Python 版是行为 oracle**，迁一块对一块，不是凭感觉重写）。本机有两处副本：macOS 树 `/Users/luxrck/Projects/pie/pie-rs`（当前实际改的这树）、WSL 树 `/mnt/d/pie-master/pie-rs`。总评估见 pie-rs/README.md。
- **已完成**（2026-09-22）：`config` / `llm`（reqwest + 手写 SSE）/ `tools` / `loop` / **`session`（JSONL + resume）** / **`context`（三级压缩 + manifest + gc）** / 一次性 CLI。44 个单测 + 真实 API 端到端跑通；含一条**契约测试**把工具 schema 与 Python 版逐字对比（基准 `pie-rs/fixtures/python-tools.json`）。
- **context 三级压缩**（2026-09-22）：语义照 Python（工具级 head/tail 指针、轮次级 user+最终输出、会话级窗口摘要 system；级别只升不降、内容 hash 落盘、软阈值/目标水位迟滞、`keep_last_steps` 保护最近 N 个 step 批次）。**消息模型不同**：Python 是类层级，Rust 是扁平 `Vec<Message>`（靠 `role` + `compress_level` + `synthetic` 判定）→ 压缩就地改写列表；周期级/会话级用切片 `splice` 替换。`Message` 新增压缩元数据字段（字段名与 Python `to_dict()` 逐字对齐）——**这些字段绝不能进 API 请求体**，发给模型前统一过 `Message::to_api()`（与 Python 同规则）。压缩的两处调用点（请求前估水位 / 请求后用 provider 上报的 `prompt_tokens`）现在直接写在 `Session::aturn` 里；会话级窗口块路径走 `on_compact` 的 `level=3` 事件回报、在回调里登记（Python 那条 `maybe_compact(..., windows=)` 链路本来就没调用方传值）。CLI：`pie-rs context info|verify|gc [--delete]`。manifest 的 `ts` 自算 ISO UTC（无日期库）、落盘 JSON 用 `indent=1`（对齐 Python）。
- **session 持久化契约**（2026-09-22）：与 Python 版**同目录同格式**（`~/.pie/sessions/*.jsonl`：首行 `__meta__` = usage/windows/cwd/title，往后每行一条消息）—— Python 写的会话 Rust 能读（未知字段忽略、旧 system 丢弃后按当前提示词重建、带 tool_calls 缺 reasoning_content 补空串），反之亦然。差异：文件名 `chat-<unix 秒>-<微秒>.jsonl`（无日期库；Python 是本地时间 `%Y%m%d-%H%M%S-%f`），两边按 mtime 选最新。**`UsageTracker` 与 Python 同语义：token 字段是最近一次上报值（不求和），只有 `calls` 累加**；`loop::run_turn` 新增 `usage: &mut UsageTracker` 参数，每次模型请求 `record`。CLI：`-s/--session <id|路径>`（存在则载入否则新建，纯名字落到 sessions 目录）、`-r/--resume`（同 cwd 优先取最新）；一次性模式不落盘。**`Session` 自己持有 `llm` / `tools`**（对齐 Python `Session.llm/tools`）：`new/load/resume` 都要把它们传进来，`aturn(&mut self, input, on_event)` 只剩 3 个参数（`--tools` 裁出来的受限工具集也就天然跟着会话走）；为此给 `ToolRegistry`/`Entry` 补了 `Debug`。
- **Session API 对齐 Python**（2026-09-22 补齐）：`set_model`/`set_reasoning_effort`（+ `Config::save` 写回 TOML：`reserved_tokens=None→"auto"`、`compaction=false`/`tool=false`，两侧对称、有 round-trip 测试）、`fetch_models`（缓存 `available_models`，不持久化）、`compact(mode)`（包 `context::compact`）、`usage_report`（`/stat` 文本，CLI `--stat` 消费）、`compression_history`（读 manifest）、`reset`。仍未做：`turn`（同步包装，Rust async-first）、`clear_window`（`/clear` 的窗口归档）、`full_history`（含原文的调试视图）。
- **❗ 修了一个真 bug**（2026-09-22）：`Session::load` 把文件里的旧 system（含 **level=3 窗口摘要**）全部丢掉，却不重建 → **resume 后窗口摘要彻底消失**，模型看不到被归档的历史。现在按 `__meta__.windows` 用 `context::build_window_summary` 重建（Python `_window_summaries` 同款）。实测 resume 后模型会顺着摘要 + 指针去 `read` 原文（自愈式回放）——这也反过来证明了「窗口摘要 + 指针」这条链有效。
- **全局记忆种子文件**（2026-09-23）：`config::ensure_global_memory()`——首跑写 `~/.pie/memory.md`（模板 `config::DEFAULT_GLOBAL_MEMORY` = `include_str!("../prompts/memory.md")`，正文与 Python `config.GLOBAL_MEMORY_TEMPLATE` **逐字一致**——用 ast 抽出来对过；已存在则**不覆盖**），在 `main::run` 开头调（= Python `ensure_config()` 里那句）。两边共用同一个 `~/.pie/memory.md`（这一机器上已有 Python 写的那份 → 不会被动），注入形状 `## 全局记忆（<绝对路径>）` + `## 项目记忆（MEMORY.md）`，没文件就不拼那块。回归：`config::ensure_global_memory_seeds_once_and_never_overwrites` / `seeded_global_memory_lands_in_the_system_prompt`。
  两个内置提示词正文都在 `prompts/` 且**小写文件名**（`prompts/system.md` = `DEFAULT_SYSTEM_PROMPT`，与 Python `config.SYSTEM_PROMPT` 逐字一致；`prompts/memory.md` = `DEFAULT_GLOBAL_MEMORY`）——别与**运行时按名字找的**仓库根 `SYSTEM.md` / `AGENTS.md` / `MEMORY.md` 搞混（本仓根根本没 `SYSTEM.md`，所以实际用内置那份）。
- **工具层定稿**（方案来回换过好几轮，最终这版）：**一个工具 = 一个结构体** —— 字段即参数、**doc 首行即 `function.description`**、非 `Option` 即必填、`#[schemars(skip)]` 即私有参数；schema 由 `#[derive(Deserialize, JsonSchema)]`（serde + schemars）派生，**不自己写宏**。协议 3 行：`trait Tool: DeserializeOwned + JsonSchema + Send + Sync + 'static { fn call(self) -> impl Future<Output=ToolResult> + Send }`；实现**直接写在 `call` 里**（用 `let Self { .. } = self;` 解构——没有 `_impl` 转发层，避免参数表与字段重复）；分发靠泛型 `erased::<T>` 单态化成**函数指针**（不经过 `dyn Tool`，所以不碰 object safety；`call(self, ctx: ToolCtx)` 的参数都是**按值**（`ToolCtx` 内部是 `Arc`，`Clone` 便宜）→ future 仍是 `'static`）；注册 `.with_tool::<Read>("read")`（**传类型**——带字段的结构体在 Rust 里不是值表达式）。
- **工具定义形状归工具层**（2026-09-22）：`{"type":"function","function":{…}}` 这个 OpenAI 线上形状就地在 `ToolRegistry::specs()` 里 `json!` 出来（原 `llm::tool_spec` 已删）；llm 层只管把整个数组塞进请求体 `tools` 键——与 Python 版一致（形状在 `Tool.definition()`）。改形状就一处。
- **工具实现自包含**（2026-09-22 定，同日再收一遍）：单个工具用到的辅助逻辑（`probe_image` / `head_output` / `write_raw` / `shell_command` / `kill_process_group` / `norm_ws` / `truncate` / `READ_HEAD_BYTES` / `lines_by_bytes`）**全部并进各自的 `call`**；模块级只剩 `format_output`（四工具共用的输出协议）＋ trait / schema / 注册表基建。**字节预算两边口径本来就不同**（Python 也这样）：`read` 的行不含换行 → 每行 `len+1`；`shell` 的行含 `\n` → 按实际字节（原先两边共用一个 `lines_by_bytes`，等于给 shell 行多算 1 字节，2026-09-22 已改回 Python 口径）。其余工具层对齐：未知工具报错带上可用列表、`with_tool` 重名断言。测试全走 `dispatch("read"/"shell", …)` 公开路径。
- **两条编译器定的规矩（别再试回去）**：① trait 里的 `async fn` 表达不出 `Send`，而注册表要把 future 装箱成 `dyn Future + Send` → trait 用 `-> impl Future + Send`（**impl 里仍可写 `async fn`**）；② `#[schemars(...)]` 必须写在 `#[derive(JsonSchema)]` **之后**。
- **工具执行进度已移除**（2026-09-22）：Python 的 `_on_progress`（TUI 边跑边看 shell 输出）先砍了——一次性 CLI 不消费，却让 3/4 的工具挂着没用的 `_ctx`。丢的只是**实时性**（输出仍完整作为工具结果返回）；现在 `Tool::call(self, ctx: ToolCtx)` 已经有 ctx 了（取消信号走它）→ 加进度时在同一处挂 `mpsc::Sender` 即可。
- **重试收敛到唯一驱动器**（2026-09-22）：`LlmClient::with_retry(what, op, decide)` 是全文件唯一的重试循环，`decide: FnMut(u32, &LlmError) -> Retry` 回答一次失败怎么办——`Retry::{Backoff（退避+占额度）, Now（立刻重来、不占额度）, Give}`；默认判定 `default_retry(max_retries, attempt, e)`（额度 + `retryable()`）。`complete` / `list_models` 用默认判定；`stream` 把自己的两条特例写在 `decide` 里（已吐过增量 → `Give`；400 拒 `stream_options` → 改写开关 + `Now`）。**三个 `*_once`（`complete_once` / `stream_once` / `list_models_once`）已退役**：一次尝试的完整流程就地写在 `with_retry` 的闭包里（
stream
 的就是闭包中那段 async 块，`[DONE]` 直接 `return Ok(state.finish())`）——对齐 Python（请求本就写在 lambda 里）、也确实少了单调用点的一层跳转。代价：`stream` 的两个开关要用 `Cell` 共享（请求闭包与判定闭包都要读写）、`on_chunk` 包一层 `RefCell`（跨多次尝试复用），`stream_once` / `StreamState::absorb` 的 `emitted` 参数从 `&mut bool` 改成 `&Cell<bool>`。（Python 版对齐：`_retry` 只包非流式，流式单独一段。）
- **图片识别用 `image` crate**（2026-09-22）：`guess_format`（魔数）+ `ImageReader::with_format(..).into_dimensions()`（只读头部、不解码全图）替掉手写的 PNG/GIF/BMP 头 + JPEG SOF 扫描 + WEBP 三变体（~60 行）。用 Pillow 真图对照过：与手写版**逐字一致**（含 TIFF/ICO/WAV 反例）。⚠ **必须过格式白名单**：`guess_format` 认得 TIFF/ICO（哪怕没开那些 feature），而 Python 版不当图片。配置 `default-features=false` + `png/jpeg/gif/webp/bmp` → +23 个纯 Rust crate、零 C、冷构建 +7.5s；clipboard 模块将来也靠它编码 PNG。
- **shell 超限保留头部而非尾部**（2026-09-22，与 Python 版有意不同）：`head_output`（原 `tail_output`）只取开头 N 行，全文照旧落盘成 `[工具输出全文已保存: …]` 指针；字节口径与 Python `_tail_output` 对齐（行含 `\n` 按实际字节）。理由：`cat 大文件` 这类命令开头才是要看的部分。
- **schemars 的四个坑**：doc 注释的**单换行会被合并成空格**（多行描述得用 `#[schemars(description=…)]`）；嵌套结构体**不能加 doc**（会在 `items` 上加出 Python 没有的 description）；归一化 = `meta_schema=None` + `inline_subschemas=true` + `option_add_null_type=false` + 递归删 `format`；`required` 是字母序（Python 是声明序，无语义差别，契约测试两边排序后比）。
- **`pie-rs/macros/` 已删**（2026-09-23）：那是上一版 `#[tool]` 属性宏的遗留（方案早废弃），连同 workspace `exclude = ["macros", …]` 里的那项一起清了——`exclude` 现在只剩 `["bindings"]`。`fixtures/python-tools.json` **不能删**：`tools.rs` 的契约测试 `generated_specs_match_python` 用 `include_str!` 编译期引用它。
- **工具层已与 Python 对齐完**（2026-09-22）：① `edit` 四类诊断齐了（级联引用 / 出现多次 / 重叠 / 只差空白 + **「原文里最接近的位置 + 简版 diff」**：锚行 = oldText 最长行、字符级 LCS 当相似度、窗口与 oldText 等长逐行对齐，自写不引依赖；超长回显用 `{:?}` = Python `!r`）；② `--tools` 工具集裁剪 + 受限 shell（`tools_from_spec(spec, defaults)`：内置名启用、非内置名当 shell 子命令白名单并隐式启用受限 shell；受限 shell **不另起工具类型**，靠 `Shell._allow_cmds`（`#[schemars(skip)]`）经 defaults 注入，description 追加白名单）。`--tools` 的子命令校验在启动进程之前（不白跑）。
- **回合循环在 `Session::aturn`（2026-09-22 合并）**：原 `loop.rs`（`run_turn`）整体并进 `session.rs` —— 两者本就是一回事的两半，分开只会让每次调用在两模块间穿 7 个参数（其中 4 个是 `self` 字段）。连带：`TurnEvent`/`dispatch_tool`/`last_assistant_text` 也搬过去（`TurnOutcome` 删了：`aturn` 直接返回 `String`，`steps` 只在内部用于 `max_steps` 判定）；`loop.rs` 与 `#[path] mod agent_loop` 一并删除；`Session` 新增 `manifest: Option<PathBuf>`（`None` = 不记账）与 `Session::ephemeral()`（一次性模式用：不落盘、不写 manifest，另加 `set_system_prompt` 支持 `--append-system-prompt`）。⚠ 与 Python 的结构差异：那边 `loop.py` 仍是独立一层（TUI/CLI 复用 `loop.run`）。
- **loop 层与 Python 对照**（2026-09-22 复核）：已对齐——两处压缩调用点（请求前估值 / 请求后用 provider 上报的 prompt_tokens）、工具结果消息带 `tool_name`、事件字段（`tool_call` 带 turn/step、`tool_result` 带 arguments 且截 500）、非法 JSON 参数回文本（`[参数解析失败] 模型返回了非法 JSON: <前500字>`）、**shell 自带落盘结果的 `compress_level=1` + `raw_path` + manifest 事件**。⚠️ 两个易漏的坑：① shell 落盘的 `shell-*.txt` 若不被 `raw_path`/manifest 引用，`context gc --delete` 会把全文删掉、历史里的指针变死链（Python 在 `loop._finalize_tool_message` 里同步，且按 `call.name == "shell"` gate——read 回来的源码字面量里可能恰好含同样格式的字符串）；② manifest/消息的 `raw_hash` 必须从**文件名**取（shell 落盘的内容是 stdout 原文，拿结果文本重算就对不上）。
- **执行旋钮收进配置**（2026-09-22）：`Config.max_steps`（`Option<usize>`，不写 = 不限）与 `Config.stream`（默认 true）；`Session::aturn` 不再收这两个形参（Python 是 `aturn(max_steps=…, stream=…)` 按次传）→ 这两个旋钮加上“回合循环已并进 `Session::aturn`”，不再有那串跨模块传参（原 8 参的 `loop::run_turn` 已删）。连带：`TurnEvent::Answer(String)`（**只在非流式时推**；流式下正文已增量推过，再推会重复——Python 是总推 answer、靠 TUI 覆盖），main.rs 收到就 `println!`。main.rs 日志也换上了 `[tNsM]` 轮/步标签，结果行带参数摘要。
- **图片 Files API**（2026-09-22）：**协议在 `LlmClient`（`llm.rs`）**：`FileObject` + `upload_file`/`list_files`（自动翻页）/`delete_file` + `model_supports_files`/`key_fingerprint`/`TTL_MAX_DAYS`；**本地文件管理在 `session.rs`**：`files_dir()`/`store_blob`（内容寻址 `img-<sha256[:16]>`，0o600）/`entry_is_usable`/`iter_session_files`/`file_id_index`/`collect_file_garbage`（24h 保护窗）/`GC_PROTECT_HOURS`。**不再有 `ImageStore` 结构体**（它只是为了绕开借用检查的包装）：改为 `Session::ensure_image_file`（落副本 → 命中记录就复用 → 否则上传 → 就地写 `self.files`）与 `Session::invalidate_file`；注入在 `Session::inject_read_images`。时间换算（`now_unix`/`iso_utc`/`fmt_unix_ts`）统一在 `config.rs`（`context` 与 `session` 共用一份）。
- **⚠ `expires_after` 的编码坑**：只能用**方括号展开的两个表单字段** `expires_after[anchor]=created_at` + `expires_after[seconds]=N`（openai SDK 的 `_serialize_multipartform(stringify_items(array_format="brackets"))` 就是这么发的）。实测把 JSON 串当**单个**字段发（part 设不设 `application/json` 都一样）→ 响应 `expires_at` 为 `null`，服务端当永久件收下（TTL 静默失效）。验证方式：`curl -F 'expires_after[anchor]=created_at' -F 'expires_after[seconds]=3600' -F file=@x.png` 看响应。
- **图片与 Python 的有意差异**：**不回退内联 base64**（用户定）。拿不到 `file_id`（未开启 / 模型不支持 / 上传失败）就不注入图（标记文本仍在工具结果里）；`file_id` 失效时也不转 base64，而是 `Session::downgrade_file_blocks` 把历史里的 `file` 块换成文本占位 + 记录标失效（下次同图重传）+ 重试一次。本地副本与 `__meta__.files` 字段名与 Python 逐字一致（两边记录互认）。
- **CLI 参数与 Python 对齐**（2026-09-22）：覆盖类 `-m/--model`、`-t/--thinking`（⚠ **`-t` 从 `--tools` 让给了 `--thinking`**，与 Python 一致；`--tools` 无短名）、`--reserved-tokens`（`N`/`64k`/`auto`，复用 `config::parse_reserved_tokens`）、`--max-steps`、`--no-stream`、`--auto-compact-threshold`（运行时覆盖软阈值，`Config::soft_limit` 里优先用）、`--timeout-seconds`、`--max-retries`、`--max-retry-delay-seconds`、`--cwd`、`--system-prompt`、`--append-system-prompt`（后两个支持“字面文本或文件路径”）；`--stat`。任务可省略 → 非 TTY 时从 **stdin** 读。子命令加 `sessions [-l/--all/--json]`。所有覆盖都是**只改内存 cfg、不写回文件**（集中的 `apply_overrides`）。仍未做：`--mode {text,json,transcript}`（结构化输出）、`setup` 子命令。
- **取消通路（`/stop` / TUI 的 Esc）**（2026-09-22）：自建 `src/cancel.rs` —— `Cancel` = `AtomicBool` + `Notify`（`Clone` 即共享），**不引 `tokio-util`**。⚠ `Notify::notify_waiters` **不补发**通知，所以 `cancelled()` 必须先查标志位再 `notified().await`（否则「先 cancel 后 await」永远等下去）。三个取消点：`Session::aturn` 每步开头查一次；模型请求 `tokio::select!` race（取消 → `model_call` 返回 `Ok(None)`，**不 push 任何消息**）；`shell` 在等子进程时 race → `killpg` 整个进程组（与超时同款）。收尾与 Python `_cancel_turn`/`_cancel_tools` 一致：未执行的 `tool_calls` 补 `CANCEL_TEXT`（=「用户手动终止」）的 tool 消息（**保证每个 `tool_call_id` 都有配对、API 序列合法**）+ 历史里写一条 `CANCEL_TEXT` 的 assistant 消息 + 作为本轮答复返回。信号经 `tools::ToolCtx { cancel }` 按值传进工具层。实测（pty 驱动真 TUI）：Esc 后 10s 内停止、工具没跑完（无 `[exit=0]`）、后续提问正常（序列合法）。TUI 侧：`/stop` 与 Esc 等价（`submit` 在 busy 时**只放行 `/stop`**，其余输入提示等回合结束）；退出时 `shutdown()` **先 cancel 再等锁保存**（否则长命令回合会让退出卡 5s 后跳过落盘）。
- **TUI 已落地**（2026-09-22）：`src/tui/`（`app.rs` 主循环 + 渲染 / `status.rs` 顶栏与活动状态 / `input.rs` 输入框 / `history.rs` 滚动历史 / `markdown.rs` Markdown 渲染缓存 / `theme.rs` 配色 / `clipboard.rs` 粘图）。依赖（用户点名的组合，2026-09-23 升到 ratatui 0.30）：`ratatui 0.30` + `crossterm 0.29` + `ratatui-textarea 0.9`（输入框，原 `tui-textarea` 停更版已换，见下面那条）+ `tui-markdown`（**关默认高亮**，否则 syntect→oniguruma 会带进 C 编译）+ `arboard`。**Python 的 `textkit.py`（CJK 断行）不需要移植**：ratatui 的 `Paragraph` 本来就按显示宽度逐字换行；`tui.py` 的盒子/简洁模式（`_box`/`_panel`/`_lean`）也没照搬，只沿用同一套配色与状态图标语义。界面特色：流式增量渲染、思考耗时显示在助手块标题、工具调用/结果按状态着色（✓/✗/⏹）、PgUp/PgDn 滚动；顶栏上下文用量形如 `53,134/920,576 (5.8%)`（2026-09-23 用户点名：千分位 + 百分比、**不带 `tok`**）。（lean 与补全：见下面两条；输入框多行 / 软换行 / bracketed paste / 告警出口 / 滚动按显示行：见 2026-09-23 那几条。）
  - **顶栏用量块“没上报就按 0 算” + 启动不推键位行**（2026-09-23 用户点名）：顶栏右侧恒为 `53,134/920,576 (5.8%)` 这个形状，provider 还没上报（新会话）就显示 `0/920,576 (0.0%)`——**不再有 `N calls` 这个回退**（位置固定、不会一会儿是次数一会儿是百分比）；只有配置没给窗口（预算 ≤ 0，没分母）才整块省略（连 `│` 分隔符也不画）。启动也不再往消息流 push 那行键位提示（底栏已在显示键位、`/help` 有完整命令表），空白历史区不再被顶掉一行。回归：`status::header_shows_zero_usage_before_any_report` / `app::fresh_screen_shows_zero_usage_and_no_notice`。
  - **Ctrl+G / `/paste` 只认图片**（2026-09-23 修，之前会兜底成文本粘贴）：两条来源、顺序与 Python `clipboard.grab_image_path()` 对齐——位图（`get_image()` → PNG 落内容寻址副本，插**副本**路径）→ **文件列表**（`get().file_list()`，覆盖 macOS 的 furl / Windows CF_HDROP / Linux URI 列表；只认图片后缀且文件存在的第一个，插**原路径、不复制**）；都没有 → `None`，**文本不兜底**（`clipboard::paste_image()` 不再调 `get_text()`）。后缀表与 Python `_IMAGE_SUFFIXES` 同款（含 TIFF）。纯文本粘贴由终端自己的键（`⌘V` / `Ctrl+Shift+V`）经 bracketed paste 走 `Event::Paste` —— 这正是 Python 的语义（Ctrl+G/`/paste` = `action_paste_image` 只图片 + 无图提示 `no_image_hint()` 对齐 `_NO_IMAGE_HINT`；只有 TextArea 的 Ctrl+V 才回退文本）。
  - ⚠️ **pty 驱动验证 TUI 时必须设 winsize**（`fcntl.ioctl(master, termios.TIOCSWINSZ, struct.pack("HHHH", 行, 列, 0, 0))`），否则 ratatui 拿不到尺寸、界面根本不出来（只看到启动前的那几行日志）。断言屏幕内容优先用 **pyte 还原屏幕**（`uv venv /tmp/ptyenv && uv pip install pyte`；`pyte.Screen(列, 行)` + `pyte.Stream(screen).feed(bytes.decode())`，`screen.display` 就是屏幕文本）——ratatui 是**双缓冲 diff 重绘**，同一字符会在原始输出里重复出现很多次，直接数 `str.count` 完全不可靠；实在要扫原始字节才先剥掉全部 CSI（`\x1b\[[0-9;?]*[ -/]*[@-~]`）再找子串。⚠️ `screen -dmS … -X hardcopy` 对**交替屏**程序导不出内容（拿到空文件），别指望它。⚠️ pyte 的 `Screen` **不隔离交替屏**：TUI 启动前写在 stderr 的横幅（如 `[session] 载入 …（3 轮历史，9 次请求）`）会残留在还原出的屏幕文本里（表现为顶栏行尾多出几个字），不是渲染 bug。
  - **输入框是多行编辑器**（2026-09-23 确认）：`ratatui-textarea` 天然多行（长行**软换行**，见下条），回车提交、`Shift+Enter`/`Ctrl+J` 换行（多数终端把 ⇧⏎ 编成 LF = `ctrl+j`，所以两个都认），内容超过 9 行才内部滚动；`Shift+Enter` 在 Terminal.app 等终端收不到（只发 `\r`）→ 用 `Ctrl+J`。
  - **输入框已改软换行**（2026-09-23，依赖换成 `ratatui-textarea`）：旧的 `tui-textarea 0.7` 已停更且**没有 wrap**（它的 `Widget` 渲染的 `Paragraph` 未设 `wrap`、ratatui 0.29 默认也是 `wrap: None`）→ 长行水平滚动、且 `desired_height` 按逻辑行数算。现在用官方接手的 `ratatui-textarea = 0.9`（原 `tui-textarea` 的继任，作者同一人；另有社区 fork `tui-textarea-2` 也是同一特性集）+ `WrapMode::Glyph`（按字形逐字断，CJK 友好且高度可精确折算）。连带升级：`ratatui 0.29 → 0.30`、`crossterm 0.28 → 0.29`、`tui-markdown 0.3.6 → 0.3.9`（**0.30 体系是全套的**：`ratatui` facade + `ratatui-core` / `ratatui-widgets` / `ratatui-crossterm`，全树 crossterm 仍只有一个）。代码只改了 3 处：`use` 改名、`Input::new_area()` 里 `set_wrap_mode`、`desired_height(area.width)`（**宽度必须由调用方传**：高度先于渲染定下）按 wrap 后的显示行数算（`ceil(显示宽/可用宽)`，用 `ratatui::text::Line::width()`）。控件自己管垂直滚动（wrap 模式下水平滚动归零）；实测 pty（宽 100、400 个 `a`）= 折成 4 行，屏幕上 a≈400（改前只有 ~100）。回归：`input::height_counts_wrapped_rows_too` / `app::long_input_line_wraps_instead_of_scrolling` / `app::wrapped_input_scrolls_to_keep_cursor_visible`。
  - **多行粘贴必须自己开 bracketed paste**（2026-09-23 修）：`ratatui::init()` 只做 raw mode + 交替屏，**不开** bracketed paste → 终端不用 `\x1b[200~` 包住粘贴内容，多行文本被拆成一个个按键（换行 = 回车）→ 在第一行就发出去。现在 `tui::run` 里自己 `Enable/DisableBracketedPaste`（与鼠标捕获同处），`app.rs` 把 `Event::Paste` 的 CRLF/CR 归一成 `\n` 再插进输入框。
  - **告警不能写 stderr**（2026-09-23 修，`src/log.rs`）：TUI 期间 raw mode + 交替屏下直接写 stderr 的字节落在**当前光标**处，而 ratatui 每帧只重写变化的单元格（双缓冲 diff）→ 砸花的那几行永远不会被重画。实测（pty + `base_url` 指向 `127.0.0.1:9` 触发重试）：eprintln 版那行 382 字符长、无前缀、把屏幕顶乱；走 `log::warn` 后是消息流里一条正常折行的 `· …`。`log.rs` 是进程级单槽 sink：`App::run` 装上（`UiEvent::Notice`）、退出卸下，没人装（一次性模式/测试）就退回 `eprintln!`；退出时的保存提示由 `App::run` 返回、`tui::run` 在 `restore()` 之后打印。
  - **滚动偏移必须按折行后的显示行算**（2026-09-23 修）：`Paragraph::scroll` 跳过的是 wrap 之后的行，拿逻辑行数算 → 长行/中文一折行就把底部内容顶出视口。显示行数现在直接用 `history::layout` 的 `rows.len()`（当晚改成自己折行，见上条；`unstable-rendered-line-info` feature 已不需要）。回归：`app.rs::bottom_stays_visible_when_lines_wrap`。
  - **输入框高度 = 文本行数 + 1，`MIN_H = 3`**（2026-09-23 用户点名「默认行高 3 行」）：只画了 `Borders::TOP` → 内容区 = 高度 - 1，所以折算只 `+1`；`MIN_H = 3` = 1 边框 + 2 行内容，**空 / 单行输入框也占 3 行**，文本下方留一行空白、不贴底栏（同日早先曾按「`+2` 是 off-by-one」压到 2，随后按用户要求改回）。回归：`single_line_input_keeps_one_blank_row_above_hint` / `input::height_follows_content_within_limits`。
  - **输入框聚焦高亮 + shell 模式边框 + 选中高亮**（2026-09-23，对齐 Python `#input:focus` / `#input.shell-mode` / `.text-area--selection`）：本 TUI 只有输入框一个可聚焦控件 → 「聚焦」在视觉上 = 输入框上边框**常亮 accent**（原先恒为 `muted` 灰、看着像没激活）；输入以 `!` 开头时切 `palette.tool()`（提示这条直接当 shell 跑、不进上下文，判据与 `App::submit` 同一处）。选中样式从控件默认的 `bg LightBlue`（深色终端下浅蓝底 + 正文色字，糊成一片）改成 `accent 底 + accent_text 字`；`Palette` 新增字段 `accent_text`（mocha `#06121f`）与 `style_accent()`。**`Ctrl+A` 已绑成全选**（控件默认拿它当「跳到行首」；Textual TextArea 是 `select_all`）；选中本身靠 `Shift+方向键`（鼠标事件被 App 拿去滚历史区了）。回归：`app::input_border_highlights_focus_and_shell_mode` / `app::ctrl_a_selects_all_so_next_input_replaces_it`。
  - **折行自己算 + 消息流框选复制**（2026-09-23）：`history::lines(...)` → **`history::layout(cells, palette, width, lean) -> Layout`**——消息流不再交给 `Paragraph::wrap`，而是自己按宽度折行（词级优先：空白留在上一行行尾 → **一个字符不丢**；超宽单词硬断）。`Row { line, src_line }` 里的 `src_line` = 逻辑行号，`Layout::slice_text(r1,c1,r2,c2)` 就靠它拼接：**同一逻辑行的相邻显示行直接接上（软换行不算换行）、不同逻辑行之间才 `\n`**（对齐 Python `SelectableRichLog` 的目标，但**不用它那套「渲染后再对齐 + 对不上就回退」的启发式**——这里知道每行是怎么折的）。列 → 字符用「起点落在选区里就整个要」（宽字符不劈开，口径同 Python `_cell_to_char`）。滚动/鼠标的显示行数就是 `rows.len()` → **`unstable-rendered-line-info` feature 已不需要（已从 Cargo.toml 去掉）**；新增依赖 `unicode-width = "0.2"`（本就是 ratatui 的传递依赖）。
    复制交互（`App::on_mouse` / `paint_selection` / `finish_selection` / `take_selection_text`）：**鼠标**捕获开着（滚轮要用）→ 终端原生选择用不了，所以自己做；屏幕坐标 → 「**绝对**显示行 + 单元格列」（`body` / `scroll_top` / `layout` 都是上一帧记下的，`Rect::default()` 初始值），拖动时贴边、消息流区域外按下不算选区；高亮在段落渲染**之后直接改 buf 的单元格**（`frame.buffer_mut()`，纯几何，不关心怎么折行）；**松开即写剪贴板**（`clipboard::copy_text`，失败提示「剪贴板不可用」）+ **右下角浮一条 Toast** `已复制 N 字符到剪贴板`（**不占消息流** = Python 版 `App.notify` 的 Textual Toast，不是先前那个消息流 notice）。Toast = `App::toast(文案, ok)` 记 `(文案, 截止时刻)`；`paint_toast` 在**所有控件之后**用 `Clear` 擦底 + `Block::bordered()` 小盒子，贴屏幕右侧与输入框上沿（`above = input.y`，不盖输入框）；`TOAST_TTL = 3s` 到点自消（主循环 66ms 一 tick → 最多差一帧；过期判定在 `render` 里，复用同一个 `Instant::now()`）。`Esc` 先收选区（防鼠标在窗口外松开丢 Up 事件）。回归：`history::wrap_never_exceeds_the_width_and_keeps_every_char` / `copy_of_a_soft_wrapped_line_stays_one_line` / `copy_breaks_between_logical_lines` / `selection_crops_by_cells_without_splitting_wide_chars` / `app::mouse_drag_copies_wrapped_line_as_one_line` / `dragging_paints_the_selection_in_accent` / `selection_uses_absolute_rows_so_it_follows_scrolling` / `escape_clears_the_selection_and_click_outside_does_not_start_one` / `copy_toast_floats_at_the_bottom_right_then_expires`。
  - ⚠️ **字符串按字节切片前必须退到字符边界**：`markdown.rs::fingerprint` 原先 `&src[..64]`，中文回答一旦超过 64 字节就 panic（`byte index N is not a char boundary`）→ 整屏崩掉、会话都不落盘。截断/指纹类代码统一走 `floor_char_boundary`（本仓自写的小函数），别用裸切片。
- **TUI lean 模式 + `/` 命令补全**（2026-09-23）：`[tui] lean` 终于被消费（`App.lean` ← `session.config.tui.lean`，默认 true）——`history::layout(cells, palette, width, lean)` 在 lean 下**成功的工具结果不留正文**（只留一行 `✓ shell(pwd)`），失败/取消才跟正文块（**统一缩进 2 空格**——2026-09-23 用户要求去掉首行的 `↳ `、续行也从 4 格改 2 格；截断到 `TOOL_BODY_LINES`=12）；`lean=false` 恒展开（盒子式）。新增 `src/tui/palette.rs`：候选表 `COMMANDS` 是**唯一事实来源**（`/help` 文案由它生成）+ `matches(input, models, model, effort)`（`/model ` / `/thinking ` 前缀 → 模型/级别候选并标「← 当前」，其余按前缀匹配表）+ `panel_lines`（最多 7 项 + 「… 还有 N 个」+ 高亮 `▸`）。App 侧：`Layout` 在输入框上方插 `panel.len()` 行的面板；`Tab` 接受候选、`↑/↓` 切候选（面板可见时优先于历史/光标）、`Esc` 先收面板再当 `/stop`（`palette_hidden` 标志，输入一变自动恢复）、回车遇半截命令先补全再提交；`App::run` 里 `spawn_fetch_models()` 后台拉模型（`UiEvent::Models` 回传）。新入口 `Input::set_text`（接受候选时替换输入）。
  - pty 验证（设 winsize）：`/co` 弹出 `/compact` 三项候选、Tab 变成 `/compact`、`/model ` 列出 `deepseek-flash ← 当前` / `deepseek-v4-pro` —— 均正常。
- **TUI `!cmd` 手动 shell 已接上**（2026-09-23，替掉原先那句「还没接」的提示）：`submit` 认 `!` 前缀 → `App::run_shell` → **不经过 LLM、不进会话上下文、不落盘**；后台 task 跑 `exec_shell(cmd, cancel)`（与工具层同款：`sh -c` + 独立进程组 + stderr dup2 进 stdout；取消 → `killpg` + `wait` 限时 3s，所以 `!sleep 30` 按 Esc 立刻收尾）。命令行回显 = `Cell::Tool { summary: "$ cmd", manual: true }`，结果经 `UiEvent::ShellDone(Status, body)` 结算成 ✓/✗/⏹，正文 `[exit=N]\n\n<out>`（取消 = `[exit=cancelled]` + `[用户手动终止]`，对齐 Python `_show_shell_result`）。`Cell::Tool` 新增 `manual` 字段 = **不吃简洁模式**（成功的正文照展，= Python 的 `lean=False`）；跑的时候占 `busy`（Esc / `/stop` 中断，输入被挡）。`/help` 第二行加 `!cmd` 说明。连带修掉「取消后工具行停在 `•`」：`ToolResult` 的 content == `CANCEL_TEXT` → `Status::Cancelled`，`TurnDone` 取消分支调 `Cell::cancel_running`。⚠ 渲染层坑：**`Line`/`Span` 里的 `\n` 不是换行**（实测 ratatui 把整段挤在同一行、`\n` 直接消失）→ `Cell::Notice` / `Cell::Error` 改成按 `text.lines()` 逐行 push（续行 2 空格缩进）；`/status` 的多行报告此前一直是挤成一行的。回归：`app::bang_runs_shell_and_stays_out_of_the_context` / `bang_shell_can_be_stopped` / `multiline_notices_break_into_lines`。
- **TUI resume 回放已接上**（2026-09-23）：`Session::full_history()` 把压缩指针展开成完整转录（level 1 → 落盘全文，level 2/3 → 原文消息序列；落盘文件没了就退回压缩形式），`tui::history::cells_from_history` 再转成 cells（system 跳过、`synthetic` 的图片消息不算用户输入、`tool_calls` 与结果按 `tool_call_id` 配对成一条工具行），`App::new` 建 cells 时一并回放（新会话只有 system → 空）。`history::tool_status`（`[exit=N]` / `CANCEL_TEXT` → ok/fail/cancelled）实时事件与回放共用。两处偏离 Python：① 展开 level 1 时**把原消息的头区拼回全文前**（`[exit=N]` 在头区、不在落盘件里；不拼的话回放里失败的命令显示成 ✓ 且不带正文）；② 不把 manifest 里「已不在消息中」的原文追加到末尾（Python 那样做只会让顺序错乱）。回归：`session::tests::full_history_expands_compaction_pointers` / `tui::history::tests::history_replay_pairs_calls_with_results` / `tui::app::tests::resume_replays_history_into_the_message_stream`。pty 实测（`-s` 指一个含 level 1/2/3 指针与 `windows` 块的会话）：回放顺序、退出码、展开正文都正确。
- 工具正文块缩进：2026-09-23 早先按「首行 `↳ ` + 续行 4 格」对齐 Python，晚些用户要求改成**统一 2 空格**（`renders_user_assistant_tool_and_thought` / `lean_hides_successful_tool_body_but_keeps_failed_one` 随之改断言）。⚠️ 超行提示 `    …（已省略 N 行）` 仍是 4 格（未动），要一致就再改一位。注：`main.rs`（一次性模式的结果行）另用 `    ↳ `，与 TUI 无关。
- **Python 绑定（`pie-rs/bindings/pie-py`，M0+M1 已落地，2026-09-23）**：PyO3 0.29 + maturin 把核心层给 Python（包名 `pie_rs`，与纯 Python `pie` 并存，**TUI 不进绑定**）；规划/进度/踩坑见 `pie-rs/docs/python-bindings.md`（§11）。已实现 `Config`/`LlmClient`/`ToolRegistry`/`Session`/`Cancel` + 事件回调 + 异常层级；`maturin develop` + `pytest`（本地假 SSE 端点，**不联网**）10 例全绿。
- **M0 结构（同日志）**：`pie-rs` 已提成 lib（`src/lib.rs`，`[lib] name = "pie_rs"`）+ bin；TUI 依赖（ratatui/crossterm/ratatui-textarea/tui-markdown/arboard/unicode-width）与 clap 全转 **optional**（features `default = ["cli", "tui"]`，bin `required-features = ["cli", "tui"]`）→ 绑定侧 `default-features = false` 时依赖图里真的没有 TUI（`cargo tree` 验过）。⚠ `main.rs` 改成 `use pie_rs::{…}`，**别再写 `mod xxx;`**（会变成第二份编译单元）。
- **未做（整体）**：`parallel_tools`（现为串行；`Config.parallel_tools` 已在）、`/clear`（windows 归档）、工具 panic 文本化、TUI 细化（代码块高亮 / 输入框内框选）。输入框内的选中靠 `Shift+方向键` / `Ctrl+A`（鼠标事件被 App 拿去滚历史区与框选消息流了）。（滚动按显示行、告警写 stderr、取消后停在 `•` 三个已于 2026-09-23 修（同日「输入框不留空白行」按用户要求回退为默认 3 行高）；`!cmd` 手动 shell、resume 历史回放、消息流框选复制同日接上。）
- **构建环境（本机特有，换机器要重做）**：cargo 不在 PATH → 用 `~/.cargo/bin`；源码在 /mnt/d（9p 慢）→ 构建时置 `CARGO_TARGET_DIR=~/.cache/pie-rs-target`；crates.io 被公司代理 MITM → `~/.cargo/config.toml` 配了 `http.cainfo=~/.cargo/certs/bundle.pem`（系统 CA + 代理链）与 `http.proxy`；**`api.deepseek.com` 没被 MITM**（系统根证书够用）。依赖只要 `build-essential`（cc）。
- **TLS 选型**：reqwest 用 `rustls-tls-native-roots`（provider = **ring**，不是 aws-lc-rs）→ 不需要 libssl-dev / pkg-config / cmake / perl（ring 的 crates.io 包自带预生成汇编，只有从 git 构建才要 perl/nasm）。
- **工具链怪癖**：本机 nightly 1.100.0 的格式串**不接受 `f` 类型**（`{x:.1f}` 报 `unknown format trait f`）→ 用 `{x:.1}`。

## 踩坑记录

### 注解与类型

- `from __future__ import annotations` 使注解变成字符串 → `@tool()` 用 `get_type_hints(fn)` 解析真实类型。
- `str | None` 的 `get_origin` 在 Python 3.13 返回 `types.UnionType`、3.14 返回 `typing.Union`（两者合一）→ 判断要写 `origin is Union or origin is types.UnionType`；全局 uv 工具环境（3.13）与项目 .venv（3.14）可能不同，跨版本改动要两环境都实测。

### 输入与终端

- **终端可能截走 Ctrl+V，应用的键盘绑定根本收不到**：Windows Terminal 把 `ctrl+v` 绑给终端侧粘贴（剪贴板是图片时什么也不发、也不产生 key 事件）→ pie 里 Ctrl+V 读剪贴板这条路在 WT 下无效，靠 `/paste` 命令兜底；GNOME Terminal / macOS Terminal / kitty 等把 Ctrl+V 交给应用的才走按键路径（终端自己的粘贴快捷键多是 Ctrl+Shift+V / Cmd+V）。
- **终端粘贴链路天生拿不到图片**：与终端交换剪贴板只有两条路——OSC 52（Textual `action_paste` 用的，仅文本）和 bracketed paste（`events.Paste`，终端只把文本发过来）。剪贴板里只有位图时（Win+Shift+S 截图就是：CF_DIB，无文本、无文件路径），粘贴事件是空的 → 想要图片只能应用自己去问系统剪贴板（`clipboard.py`）。
- **回退后端要用「能力缺失」信号，不能用「这次没拿到」信号**：Pillow 在 Linux 上没有 wl-paste/xclip 时抛 `NotImplementedError` —— 这才是「Pillow 看不到剪贴板」；它返回 None 只是「剪贴板里没有图」。拿 None 当触发条件会让每次纯文本粘贴都白等一次 PowerShell 冷启动（0.4s）。
- **WSLg 的剪贴板桥会转发图片**：Windows 剪贴板里的位图能以 `image/png` 被 `wl-paste` 读到（实测 0.035s、223x258 原样），所以 WSL 里装了 `wl-clipboard` 就够，不必绕 PowerShell。
- **Pillow `ImageGrab.grabclipboard()` 各平台语义（Pillow 12.3 实测源码）**：Windows 返回 `Image`（png/DIB）**或 `list[str]`**（剪贴板里是文件 = CF_HDROP）；macOS 走 `osascript -e "get the clipboard as «class PNGf»"`（**只认 PNG**，截图到剪贴板虽是 TIFF，但 pasteboard 读取时会自动转 PNG；读剪贴板不需要自动化权限，写才要）；Linux 转调 `wl-paste -t image` / `xclip -t image/png -o`，**缺工具直接 raise NotImplementedError**，剪贴板空/无图时按 stderr 关键字识别后返回 None（其它子进程错误抛 ChildProcessError）。→ 封装必须 catch `(NotImplementedError, OSError, ...)`，并按「Image / list / None」三分支处理。
- **macOS「截图粘不进来」首先是快捷键的坑，不是代码**：`⇧⌘4` 是**存成文件**（默认桌面），要直接进剪贴板得按 **`⌃⇧⌘4`**。另有一个真缺口已补：Finder 里 `⌘C` 复制图片文件时剪贴板里是**文件引用**（`«class furl»`），而 Pillow 的 macOS 分支只请求 `«class PNGf»`（位图）→ 返回 None，被当成「没有图片」。现在 `clipboard._mac_file_path()` 在 Pillow 拿不到位图时用 osascript 读 furl（`POSIX path of (the clipboard as «class furl»)`，脚本自带 `try/on error` 返回空串）→ 直接用那个文件（不复制，只认图片后缀）。无图时的提示按平台给（`tui._NO_IMAGE_HINT`：macOS 写快捷键、Linux 写 wl-clipboard/xclip）。实测：真剪贴板放位图 → 0.78s 落盘；Finder 式 furl → 返回原路径。回归：`tests/test_clipboard.py::test_mac_file_reference_returns_path`（新增 `_mac_env`/`_mac_off` 两个替身；**“没有图片”类用例都要套 `_mac_off()`**，否则在 macOS 上会真去读用户剪贴板）。
- Textual TextArea 在 ansi 主题（App 用 ansi-dark）下选中高亮会多一条 `text-style: reverse`（来自 `&:ansi .text-area--selection`）：`#input .text-area--selection` 这类 ID 规则能覆盖 background/color，但**覆盖不了没声明的 text-style**（未声明 ≠ 清除）→ 选中呈反色，和 #log 鼠标框选（accent 底 + accent_text 字）看着正好相反。要统一必须显式写 `text-style: none`。
- TUI 长流式文本别塞非滚动 Static（超出可视区的行被裁掉，视觉停更）→ 窗口化尾部或换 RichLog auto_scroll。
- **RichLog 里 Markdown 的颜色不受 Textual 主题 / palette 控制**：RichLog 用 `App.console` 渲染，而 Textual 创建的该 console **没设 theme** → Rich 的 `DEFAULT_STYLES` 生效（`markdown.code = bold cyan on black`、`markdown.h2 = underline magenta`、表格/列表青、链接亮蓝）→ 行内代码/代码块是黑底 + 终端 ANSI 青，浅色终端下糊成一片。2026-09-14 曾用 `Theme.rich_styles()` + `App.console.push_theme(...)` 把 Markdown 全量接管到 palette（含 `code_bg`/`code_fg`/`code_theme`，浅色变体配 `friendly`、深色配 `monokai`），**后按用户要求回退**（字段/方法已删）。如今只留**最小覆盖**：palette 字段 `markdown_code`（**完整样式串**，空串 = 不覆盖）+ `Theme.markdown_styles()`，**只有浅色变体 latte 有值**（现为 `"bold cyan"`——去底色只留青色粗体字；`markdown_styles()` **原样使用**该值，不再自动拼 `bold`），`on_mount` 非空时 `push_theme` 只改 `markdown.code` / `markdown.code_block` 两键；mocha 不注入，继续走 Rich 默认。fence（带语言标注的代码块）底色来自高亮主题的 token（`#272822`），`markdown.code_block` 治不到——另用 palette 字段 **`code_theme`**（pygments 主题名）经 `Markdown(code_theme=...)` 传入：mocha = `monokai`（= Rich 默认，等于不动）、latte = `solarized-light`（暖白底 `#fdf6e3`，用户手改自早期的 `friendly`）。回归：`tests/test_tui.py::test_markdown_code_styles` + `tests/test_theme.py::test_markdown_styles_are_minimal` / `test_code_theme_follows_variant`。
- **工具输出进 RichLog 前必须清洗控制符/ANSI 转义**：Rich 排版把不可见转义字节计成宽度（`cell_len("\x1b[01;32mA\x1b[0m")` = 10 对可见 1）→ Panel 量出的内容宽比实际大，盒子的顶/底边框与内容行右边框不在一列（`ls --color` 必现）。清洗走 textkit.py 的 `strip_escapes()` / `rich_text()`（SGR 交 `Text.from_ansi` 解成样式，其余转义 + C0 剔除，`\r\n`→`\n`、孤立 `\r` 删）。顺序要紧：**先剔转义再剔控制符**，否则 OSC 的结束符 BEL(`\x07`) 先没，`\x1b]...` 的匹配会吞掉后面全部文本。
- `input()` 在部分终端（WSL/mintty）退格按字节删中文会截断 → 交互式聊天改用 prompt_toolkit 行编辑（`cli.read_input`）。例外：首次运行向导 `config._prompt` 用内置 `input()`（答案都是 ASCII：模型名 / URL / key）。
- **框选复制要按「源文本」切，不能按显示行拼**：`RichLog.lines` 是软换行后的显示行，且 Rich 在盒内换行点会直接吃掉那个空格（`'aaaaaa bbbb cccc' + 'dddd…'`）→ 按显示行拼接既多出换行又丢空格。`tui.py` 的 `SelectableRichLog` 每次 write 记 `_CopyEntry(row, count, src, spans)`，复制时按字符区间切 src（同一 src 的相邻显示行合并 → 空格/换行都回来），无映射才回退。src 取法：Panel→盒内正文、Text→`.plain`（先 `expand_tabs()`，与显示一致，含 tab 的输出才对得上）、Markdown→宽渲染(4096)纯文本（`.markup` 与显示行对不上）。对齐失败即回退，不猜。两个必须容忍的显示装饰（实测踩坑）：Rich 给 Markdown **列表续行加悬挂缩进**（源文本里没有），给**引用每行重复加 `▌ `**（源文本只首个有）——故行首空白一律不计内容（只计入 x0），整行匹配失败时再试去掉行首 `▌` 并把忽略字符数记进 span `(start, end, off)`。否则一个列表项就能让整条消息回退。分隔线 `---` 会被渲染成随宽度铺满的规则线 → 无宽度无关源文本 → 回退（可接受）。
- 显示层换行：已改成 **CJK 友好断行**——`textkit.py` 的 `install_cjk_wrap()`（tui.py 导入时调用）把 `rich.text.divide_line` 换成 `cjk_divide_line`（全角字 `cell_len == 2` 各自成一个 token，非全角串仍按词，其余逻辑同 Rich；纯 ASCII 交回原实现逐字节不变）。否则 Rich 只按空白断行：中文长句没空格 → 整段挪到下一行、上一行留白（宽 74 实测只用 42 格）。零宽断点（U+200B）无效：Python `\s` 不匹它、Rich 分词认不出。
- **输入框（`#input`/TextArea）的断行是另一条链路**：`WrappedDocument` 调 `textual._wrap.compute_wrap_offsets`，`install_cjk_wrap()` 也替换了它（`textkit.cjk_compute_wrap_offsets`）。两点约定：
  - **逐字符断**（用户要求）：`cjk_divide_line(..., char_level=True)` —— 非全角字也逐个成 token，英文单词/长路径会在当前行剩余空间里接着填（`... mixed with eng|lish words ...`），不再整块挪到下一行留一大片空白。Rich 日志区仍是词级（代码/命令整词更好读）——**两者行为不同是有意的**（2026-09 用户确认：日志区暂不改）。含 tab 的行仍交回 Textual 原实现（tab 宽度依赖列位置，用它预计算的 `precomputed_tab_sections`）。
  - ⚠️ **「尾空白算不算进词宽」两家不同**：Rich 的 `divide_line` 按 `word.rstrip()` 判宽，Textual 的 `compute_wrap_offsets` 把 `chunk`（**含尾空白**）整段算进 `remaining_space`。曾让两者共用 `cjk_divide_line` → 含中文的行遇到**行尾空格**时会产出 `width + 1` 格的显示行（400 条随机混排实测 3 条超宽，Textual 原版 0 条），输入框里光标/滚动位置随之偏 1 格。现在 `cjk_divide_line(..., count_trailing_space=)` 参数化：Rich 链路默认 `False`，Textual 链路传 `True`。
  - 回归：`tests/test_tui.py::test_textarea_wrap_never_exceeds_width`（fuzz：行宽 ≤ width）、`test_textarea_wrap_breaks_ascii_at_char`（英文也逐字符断 + 行被填满）。
- 命令行参数有长度上限，长任务走 stdin 或文件指针，不要塞进 argv。

### 会话与文件

- 会话文件名秒级时间戳同秒碰撞会互相覆盖 → 文件名加微秒 `%f`。
- `/save` 自定义路径时 manifest 按文件名 stem 查找可能错位（已知边缘问题，待修）。

### 子进程与取消（/stop）

- **agent shell 工具必须 `create_subprocess_shell(..., start_new_session=True)` + killpg 杀进程组**：只 `proc.kill()` 杀 `/bin/sh` 时子孙进程（真正干活的）变孤儿并持有 stdout 管道写端，Python 3.12+ asyncio 的 `wait()` 要等 stdio 管道 EOF 才返回 → 取消/超时被卡到子孙自然退出（实测 sleep 300 卡 299s）。终止统一走 `_terminate_proc_group`（killpg SIGKILL 整个进程组，pipe 立即 EOF）；kill 后 `wait()` 限时 3s，防 D-state（不可中断内核态）进程拖死取消/超时路径。TUI !shell 的 `_exec_shell_async` 同款（start_new_session + killpg + wait 限时）。
- **loop._wait_cancellable 取消后不无限等清理**：`await asyncio.wait({task}, timeout=CANCEL_GRACE=3.0)`，超时则清理后台继续、先终止回合；别用 `asyncio.gather(task, ..., return_exceptions=True)` 无限等 task 结束。

### 上下文压缩

- `keep` 类参数要 clamp `max(1, keep)`（0 等价 1，当前轮必须保留），否则 `user_idx[m]` 越界崩。
- 轮次进行中不做 pair 提取、pair 仅当轮次以 assistant 结尾时提取，否则产生 user→纯文本 assistant→tool_calls→tool 非法序列被 DeepSeek 400。
- 工具返回全文，落盘统一在 harness 边界做（eager spill），工具内部不截断（截断会丢信息）。—— 例外：shell 支持配置容量上限（_max_lines/_max_bytes），截断时用 `write_raw` 落盘全文并返回独立指针标记，信息不丢。指针格式必须是独立的 `[工具输出全文已保存: path]`（`[` 紧挨可选前缀 + `全文已保存:`），不能嵌在其它文字里，否则 `extract_spill_path`（`_SPILL_RE`）匹配不到。
- **read 的容量上限截断不落盘**（2026-09-21 改）：它的内容**可再生**（原文件还在、自带 offset 分页）→ 只补 `[已截断：可用 offset=N 继续读]`。原先落盘是「谁截断谁落盘」的无脑套用，代价是白占一份大文件副本、且指针会被 `pie context gc` 删成死链（那份副本没有任何消费者：`extract_spill_path` 的调用点被 `call.name == "shell"` gate 住、`full_history()` 只按消息 `raw_path` 字段展开）。判据：**输出不可再生（shell 的 stdout）才落盘**。

### 事件循环与异步生成器

- **收尾的 `RuntimeError: generator didn't stop after athrow()` 是 httpx2/httpcore2 的收尾顺序问题，不是我们的 bug**（`pie -p 你好` 即可复现，长驻循环不会）：openai 流式响应读到 SSE `[DONE]` 就地 break → 「响应字节流」那串异步生成器（`AsyncStream.__stream__` → … → `PoolByteStream.__aiter__` → `HTTP11ConnectionByteStream.__aiter__` → `safe_async_iterate`）一直挂起在 yield 上；`asyncio.run` 收尾的 `shutdown_asyncgens()` 按 `loop._asyncgens`（WeakSet，顺序随对象地址漂移，所以时好时坏）一次性 aclose，**内层先关**时 httpcore2 就抛 RuntimeError。连接此时已被正确释放（`HTTP11ConnectionByteStream.aclose()` 在抛错前已调用），纯噪音。
- 对策：同步入口统一走 **`aio.run`**（不是 `asyncio.run`）——它收尾前先 `close_asyncgens()`：分多轮、每轮先摘空 `loop._asyncgens` 再逐个 `aclose()`、单个失败留给下一轮（与顺序无关，实测 2 轮清空）。**别在长驻循环（TUI）里按回合调它**：那会把同循环里无关的在用生成器一并关掉；TUI 只在 **`run_tui` 退出时**用 `aio.event_loop()`（自建循环交给 `App.run(loop=…)`）收一次尾。
- 经验：凡是「收尾才出现、还时有时无」的 asyncio 报错，先怀疑**关闭顺序 / GC 时机**；`loop._asyncgens`（CPython 内部结构，3.6+ 稳定，拿不到就降级为原生行为）就是 `shutdown_asyncgens()` 用的那个集合。

### API / 模型（DeepSeek thinking）


- **Vision / Files API 实测（2026-09）**：上传 `POST /files`（purpose=user_data）后，用 `{"type":"file","file_id":…}` 引用，`deepseek-flash` 确实看得到图；**prompt_tokens 与内联 base64 完全一致**（计费按尺寸，单图 ≤1024，与编码无关）——换 Files API 省的是**请求体/重复传输/上限**，不是钱（3 MB 图：内联 body 4 MiB ↔ file_id 187 B）。
- **`expires_after` 要进 `extra_body`**（openai SDK 不认这个 DeepSeek 扩展字段），带上后响应才有 `expires_at`；**服务端不做内容去重**（同图传两次 = 两个 file_id），所以“不重传”完全靠本地记录。
- **重试自己实现，SDK 那套已关（2026-09-17）**：SDK 的 `max_retries` 只在「发请求 + 拿响应头」阶段生效（读 body 中途断了不重试、也不包成 `APIConnectionError`），而且 pie 里还叠着一层 `stream_options` 兼容回退（catch 一切、只要还没吐过块）→ `max_retries=2` 实测最坏发 **6** 次请求（`x-stainless-retry-count` 会归零，说明进了第二轮）。现在：`OpenAILLM` 把 `client_kwargs["max_retries"]=0`，改为 `1 + Config.max_retries` 次尝试，单次等待 = `max(1.0, random(0, Config.max_retry_delay_seconds))`（默认 1.0 → 恒 1s），`_retryable` 只认 408/409/429/5xx 与 `_TRANSIENT_MODULES`（openai/httpx/httpcore/ssl）的异常；**流式只在还没 yield 过任何增量时**才重试（吐过再重来会重复内容）；`stream_options` 回退收紧成「只认 400」才摘参数重来（不占重试额度）。默认值常量 `DEFAULT_MAX_RETRY_DELAY_SECONDS` 在 **config.py**，`OpenAILLM` 同名参数可覆盖；**`OpenAILLM` 的 `max_retries` / `max_retry_delay_seconds` 不传就是 0**（组件默认不重试，无 `DEFAULT_MAX_RETRIES` 这类常量；应用里三个构造点都显式传 cfg）。
- **失效 file_id 的报错可识别**：`400 … the following file_ids do not exist or are not created under your account`（`files.is_stale_file_error` 认它）→ 触发“把历史里的 file 块降级成内联 + 重试一次”。
- **上下文窗口与输出预留是两笔账**：服务端按「输入 tokens + `max_tokens` ≤ 窗口」**预检**，超了直接 400（`BadRequestError`）且 `loop` 不兜底 → 整个回合被打断。所以压缩软阈值必须相对 `context_window - reserved_tokens` 算（2026-09 已改），按整个窗口 × 比例会在输入还没到水位时就把请求发超限（实例：793,513 输入 + 256,000 预留 = 1,049,513 > 1,048,576）。
- **窗口大小服务端只在超限报错里告诉你**：`GET /models` 只返回 id（无 context_length）；本部署 deepseek-flash 实测窗口 1,048,576、`max_tokens` 合法区间 `[1, 393216]`。

- OpenAILLM 懒创建连接池：首次真实请求在事件循环内同步建连（WSL2 实测 ~6s）阻塞 UI → `on_mount` 后台 prewarm 极小请求。
- thinking 模式要求 assistant tool_calls 消息回传时必须带 `reasoning_content`（`is not None` 判断、空串兜底），缺失报 400。

### 配置清理

- `save_history`、`resolve_config` 的 OPENAI_* env 覆盖、OpenAILLM 构造器 env 回退均为死配置/遗留，已移除；配置只从 `~/.pie/config.toml` 读取。

### 用量统计

- resume 后 `/stat` 累计显示 0（UsageTracker 不持久化）→ 会话 JSONL 首行 `__meta__` 保存用量，load 时恢复。

### Python 绑定（PyO3 0.29，细节见 `pie-rs/docs/python-bindings.md` §11）

- **GIL 释放改名了**：`Python::with_gil` / `allow_threads` → **`Python::attach` / `py.detach`**；`detach` 的闭包要 `Ungil`，**按引用捕获**时被捕获类型得 `Sync` → `mpsc::Receiver`（`Send + !Sync`）编译不过，要把 receiver **按值**进闭包再带出来。
- **`cargo build` 直接编 cdylib 在 macOS 会报一堆 `_PyBaseObject_Type` undefined**（扩展模块本就不该解析 Python 符号）→ 构建/安装一律走 `maturin develop` / `maturin build`（maturin 1.15 实测可用，`uvx --from maturin maturin` 即可）。
- `bool::into_pyobject` 给的是 `Borrowed<PyBool>`（要 `.to_owned()` 再 `.into_any().unbind()`）；给异常实例挂属性得 `py.get_type::<E>().call1((msg,)) → setattr → PyErr::from_value`（`PyErr::new_err` 拿不到实例）。
- 核心层的两个现实（写绑定时才发现）：**`Config::default()` 里 compaction 是开的**（`Some(...)`，与“不写就不压”的旧印象相反）；**`Config` / `CompactStats` 没实现 `Serialize`** → 绑定里 `to_dict()` / 统计 dict 得手工拼。

## 已知问题 / 待办

- 剪贴板粘贴的 **WSL 后端已实现**（`clipboard._wsl_png_bytes`：`powershell.exe -sta` + `Clipboard::GetImage()`，~0.5s）。实测本机更省：装了 `wl-clipboard` 后走 WSLg 剪贴板桥（`wl-paste` 拿 `image/png`，0.035s），PowerShell 那条根本不会被触发。
- 仓库级回归测试在 `tests/test_tui.py`（`uv run python tests/test_tui.py`，无需 pytest）：覆盖框选复制保真、CJK 断行、工具/命令渲染冒烟；改 TUI / 复制 / 断行后必跑。
- `PieApp._command` 仍是 86 行 if/elif（12 个分支）——可拆 `_cmd_*` 分组方法（可选，收益以可读性为主）；两个流式面板（#stream / #assistant-stream）各维护一份 tail 窗口状态机，可合并（风险中等）。

- 上下文估算用 chars/4 对中文严重低估（实例：估算 1,792 vs provider 上报 30,609）→ `/stat` 主行应优先展示 provider 上报值（`last_prompt_tokens` 已采集，主行待改）。
- `write(content=全文)` 参数 + thinking reasoning 体积大是上下文主要消耗源，且不在 spill 统计口径内；决策：留给自动压缩处理。
- `resume` 只能恢复最近一次会话，不支持选择历史会话。
- 上下文窗口 `context_window` 仍是手填，没与真实窗口校验/自动探测：400 里的 `maximum context length is N` 是最省的探测源，可做「报错 → 写回配置 → 强制压缩 → 重试」自愈（图片那条 file_id 自愈已做）。
- `maybe_compact(..., windows=)` 与 `compact(session=..., windows=)` 这条链路**没有调用方传值**（`loop.py` 只传到 `manifest`）→ 自动触发的会话级压缩会把窗口块写进 `context/` 但**不登记到 `session.windows`**（只有 `/clear` 会登记）；影响 resume 后的窗口摘要重建。
- **pie-rs 测试偶发假失败（macOS，2026-09-22 查了一阵没定论）**：并行跑 `cargo test` 时，**刚 spawn 的子进程偶尔会立刻以 SIGKILL 状态返回**（`status.code() == None` → `[exit=-1]`），表现为 `shell_reports_nonzero_exit_code` / `shell_merges_stderr_and_reports_exit` / `shell_timeout_kills_the_whole_process_group` 随机挂一个（单跑或 `--test-threads=1` 永远通过；`shell_` 过滤跑 0/14）。实测证据：spawn 后立刻 `getpgid(pid)` 返回 -1（进程已没了）、状态是 `unix_wait_status(9)`；`child_pgid == pid`（setpgid 生效、进程组隔离正常）；**跳过唯一会 killpg 的超时用例也会出现**，纯 `std::process` 压力（750 次 spawn）不复现 → 与重构无关，怀疑是 macOS / 多 runtime 并行下的环境问题。跑测试挂了先重跑一次；要稳定可加 `--test-threads=1`。


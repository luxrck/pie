[project]
name = "pie"
version = "0.1.0"
description = "极简 agent harness：内置 read / edit / write / shell 四个工具，YOLO 模式"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "openai>=1.40",
    "pillow>=12.3.0",
    "prompt_toolkit>=3.0",
    "rich>=13.0",
    "textual>=0.60",
]

[project.scripts]
pie = "pie.cli:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

